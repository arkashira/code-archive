# Code Archive
A simple CLI tool for backing up and restoring data.

## Usage
* Backup data: `python -m src.code_archive backup --data '{"key": "value"}'`
* Restore data: `python -m src.code_archive restore --file backup.json`

## Example
Backup some data:
